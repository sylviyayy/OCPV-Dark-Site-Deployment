# CoE ImageSet — OpenShift 4.22 + OCP-V
# Trimmed from community reference: eanylin/openshift-lab imageset-config-4.22.yaml
# Use for partner CoE / demo mirrors. For minimal install use imageset-config.yaml.template
#
# Apply: oc mirror -c mirror/imageset-ocpv-coe.yaml --workspace file://${OC_MIRROR_WORKDIR} --v2

kind: ImageSetConfiguration
apiVersion: mirror.openshift.io/v2alpha1
mirror:
  platform:
    channels:
      - name: stable-4.22
        minVersion: 4.22.2
        maxVersion: 4.22.2
        type: ocp
    graph: true
  operators:
    - catalog: registry.redhat.io/redhat/redhat-operator-index:v4.22
      packages:
        - name: kubevirt-hyperconverged
          channels:
            - name: stable
        - name: kubernetes-nmstate-operator
          channels:
            - name: stable
        - name: lvms-operator
          channels:
            - name: stable-4.22
        - name: local-storage-operator
          channels:
            - name: stable
        - name: openshift-cert-manager-operator
          channels:
            - name: stable-v1
  additionalImages:
    - name: registry.redhat.io/rhel9/rhel-guest-image:latest
    - name: registry.redhat.io/ubi9/ubi:latest
    - name: quay.io/containerdisks/centos-stream:9
